import { Box } from '@mui/material'

// This is not yet done. The box is to be divided into sections
// for each component and styled later on.
// Deadline for sections: 21/08

export function Profile() {
  return (
    <Box
      sx={{
        background: '#e4f7fb',
        borderRadius: '5px',
        margin: '10px',
        padding: '15px',
        height: '92.5vh',
      }}
    >
      I'm a Profile Page. Add my components here.
    </Box>
  )
}
